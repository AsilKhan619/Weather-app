# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# uv
uv.lock.bak

# Env / secrets
.env
!.env.example

# Data
data/lake/
*.parquet

# Eval runs: one JSON per run stays local; evals/results/history.jsonl is the tracked record
evals/results/*.json

# Airflow
orchestration/airflow/logs/
orchestration/airflow/*.db

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code (session/machine-local only; keep repo-shared config if any is added later)
.claude/
